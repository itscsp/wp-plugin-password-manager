import React from 'react';
import styled from 'styled-components';

const PasswordList = ({ credentials }) => {
  return (
    <>
        <p>Password List</p>
    </>
  );
};

export default PasswordList;

/* Styles for PasswordList component */
